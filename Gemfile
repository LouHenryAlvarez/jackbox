source 'https://rubygems.org'

group :development do
 	gem 'rake'
	gem 'redis'
	if RUBY_VERSION < '2.0.0'
		gem 'debugger'
	else
		gem 'byebug', '~> 2.7', '>= 2.7.0'
	end
end

# Specify your gem's dependencies in jackbox.gemspec
gemspec
